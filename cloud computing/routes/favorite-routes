const express = require('express');
const routes = express.Router();

const favoriteController = require('../controllers/favorite-controllers');

routes.post('/addFavorite', favoriteController.addFavoriteBook);
routes.delete('/removeFavorite', favoriteController.removeFavoriteBook);
routes.get('/getFavorite', favoriteController.getFavoriteBooks);

module.exports = routes;